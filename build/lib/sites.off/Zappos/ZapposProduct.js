// Generated by CoffeeScript 1.7.1
define(function() {
  var ZapposProduct;
  return ZapposProduct = (function() {
    function ZapposProduct(product) {
      this.product = product;
    }

    ZapposProduct.prototype.previewLayout = 'pictures';

    ZapposProduct.prototype.images = function(cb) {
      return this.product["with"]('more', (function(_this) {
        return function(more) {
          var id, image, images, name, style, _ref, _ref1;
          images = {};
          _ref = more.styles;
          for (id in _ref) {
            style = _ref[id];
            images[id] = [];
            _ref1 = style.images;
            for (name in _ref1) {
              image = _ref1[name];
              if (image.MULTIVIEW) {
                images[id].push({
                  small: image.MULTIVIEW,
                  medium: image.MULTIVIEW,
                  large: image['4x'],
                  larger: image['4x'],
                  full: image['4x']
                });
              }
            }
          }
          return cb(images, more.currentStyle);
        };
      })(this));
    };

    return ZapposProduct;

  })();
});

//# sourceMappingURL=ZapposProduct.map
