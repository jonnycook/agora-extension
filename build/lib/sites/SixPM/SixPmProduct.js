// Generated by CoffeeScript 1.7.1
var __hasProp = {}.hasOwnProperty,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

define(['scraping/SiteProduct'], function(SiteProduct) {
  var SixPMProduct;
  return SixPMProduct = (function(_super) {
    __extends(SixPMProduct, _super);

    function SixPMProduct() {
      return SixPMProduct.__super__.constructor.apply(this, arguments);
    }

    SixPMProduct.prototype.images = function(cb) {
      return this.product["with"]('more', (function(_this) {
        return function(more) {
          var id, image, images, name, style, _ref, _ref1;
          images = {};
          _ref = more.styles;
          for (id in _ref) {
            style = _ref[id];
            images[id] = [];
            _ref1 = style.images;
            for (name in _ref1) {
              image = _ref1[name];
              if (image.MULTIVIEW) {
                images[id].push({
                  small: image.MULTIVIEW,
                  medium: image.MULTIVIEW,
                  large: image['4x'],
                  larger: image['4x'],
                  full: image['4x']
                });
              }
            }
          }
          return cb(images, more.currentStyle);
        };
      })(this));
    };

    SixPMProduct.prototype.widgets = function(cb) {
      return this.product["with"]('more', 'reviews', (function(_this) {
        return function(more, reviews) {
          var widgets;
          widgets = _this.genWidgets(more, {
            productDetails: 'Description',
            sizes: 'Sizes',
            colors: {
              title: 'Colors',
              map: function(color) {
                return color.name;
              }
            }
          });
          return cb(widgets);
        };
      })(this));
    };

    return SixPMProduct;

  })(SiteProduct);
});

//# sourceMappingURL=SixPMProduct.map
