// Generated by CoffeeScript 1.10.0
var extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty;

define(['scraping/SiteProduct'], function(SiteProduct) {
  var SixPMProduct;
  return SixPMProduct = (function(superClass) {
    extend(SixPMProduct, superClass);

    function SixPMProduct() {
      return SixPMProduct.__super__.constructor.apply(this, arguments);
    }

    SixPMProduct.prototype.images = function(cb) {
      return this.product["with"]('more', (function(_this) {
        return function(more) {
          var id, image, images, name, ref, ref1, style;
          images = {};
          ref = more.styles;
          for (id in ref) {
            style = ref[id];
            images[id] = [];
            ref1 = style.images;
            for (name in ref1) {
              image = ref1[name];
              if (image.MULTIVIEW) {
                images[id].push({
                  small: image.MULTIVIEW,
                  medium: image.MULTIVIEW,
                  large: image['4x'],
                  larger: image['4x'],
                  full: image['4x']
                });
              }
            }
          }
          return cb(images, more.currentStyle);
        };
      })(this));
    };

    SixPMProduct.prototype.widgets = function(cb) {
      return this.product["with"]('more', 'reviews', (function(_this) {
        return function(more, reviews) {
          var widgets;
          widgets = _this.genWidgets(more, {
            productDetails: 'Description',
            sizes: 'Sizes',
            colors: {
              title: 'Colors',
              map: function(color) {
                return color.name;
              }
            }
          });
          return cb(widgets);
        };
      })(this));
    };

    return SixPMProduct;

  })(SiteProduct);
});

//# sourceMappingURL=SixPMProduct.js.map
