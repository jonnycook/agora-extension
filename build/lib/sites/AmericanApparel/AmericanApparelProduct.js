// Generated by CoffeeScript 1.10.0
var extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty;

define(['scraping/SiteProduct', 'util'], function(SiteProduct, util) {
  var AmericanApparelProduct;
  return AmericanApparelProduct = (function(superClass) {
    extend(AmericanApparelProduct, superClass);

    function AmericanApparelProduct() {
      return AmericanApparelProduct.__super__.constructor.apply(this, arguments);
    }

    AmericanApparelProduct.prototype.images = function(cb) {
      return this.product["with"]('more', (function(_this) {
        return function(more) {
          var color, i, image, len, otherImages, ref;
          otherImages = (function() {
            var i, len, ref, results;
            ref = more.images;
            results = [];
            for (i = 0, len = ref.length; i < len; i++) {
              image = ref[i];
              results.push({
                small: image.replace(/(_\d{2})/, "$1t"),
                medium: image,
                large: image,
                larger: image,
                full: image
              });
            }
            return results;
          })();
          ref = more.colors;
          for (i = 0, len = ref.length; i < len; i++) {
            color = ref[i];
            images[color.name] = [
              {
                small: color.image.replace(/(_\d{2})/, "$1t"),
                medium: color.image,
                large: color.image,
                larger: color.image,
                full: color.image
              }
            ].concat(otherImages);
          }
          return cb(images, more.color);
        };
      })(this));
    };

    AmericanApparelProduct.prototype.reviews = function(cb) {
      return this.product["with"]('reviews', function(reviews) {
        return cb({
          reviews: reviews != null ? reviews : []
        });
      });
    };

    AmericanApparelProduct.prototype.widgets = function(cb) {
      return this.product["with"]('more', 'reviews', (function(_this) {
        return function(more, reviews) {
          var widgets;
          widgets = _this.genWidgets(more, {
            description: 'Description',
            sizes: 'Sizes',
            details: 'Details',
            colors: {
              title: 'Colors',
              map: function(color) {
                return util.ucfirst(color.name);
              }
            },
            reviews: {
              obj: reviews
            }
          });
          return cb(widgets);
        };
      })(this));
    };

    return AmericanApparelProduct;

  })(SiteProduct);
});

//# sourceMappingURL=AmericanApparelProduct.js.map
