// Generated by CoffeeScript 1.10.0
var extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty;

define(function() {
  return {
    d: ['SiteInjector', 'views/ShoppingBarView'],
    c: function() {
      var MacysSiteInjector;
      return MacysSiteInjector = (function(superClass) {
        extend(MacysSiteInjector, superClass);

        function MacysSiteInjector() {
          return MacysSiteInjector.__super__.constructor.apply(this, arguments);
        }

        MacysSiteInjector.prototype.siteName = 'Macys';

        MacysSiteInjector.prototype.run = function() {
          return this.initPage((function(_this) {
            return function() {
              var initProducts, parseSid;
              _this.shoppingBarView = new ShoppingBarView(_this.contentScript);
              _this.shoppingBarView.el.appendTo(document.body);
              _this.shoppingBarView.represent();
              parseSid = function(href) {
                var matches;
                matches = /^http:\/\/[^.]*\.macys\.com\/shop\/product\/[^?]*\?ID=(\d*)/.exec(href);
                if (matches) {
                  return matches[1];
                }
              };
              window.initProducts = initProducts = function() {
                var a, href, i, img, len, ref, results, sid;
                ref = $('a img');
                results = [];
                for (i = 0, len = ref.length; i < len; i++) {
                  img = ref[i];
                  a = $(img).parents('a');
                  if (a.attr('href') && a.attr('href')[0] !== '#') {
                    href = a.prop('href');
                    sid = parseSid(href);
                    if (sid) {
                      results.push(_this.initProductEl(img, {
                        productSid: sid
                      }));
                    } else {
                      results.push(void 0);
                    }
                  } else {
                    results.push(void 0);
                  }
                }
                return results;
              };
              $(initProducts);
              $(window).load(initProducts);
              setInterval(initProducts, 2000);
              return $(function() {
                var currentSid;
                if ($('meta[name="twitter:card"]').attr('value') === 'Product') {
                  currentSid = function() {
                    return $('meta[itemprop="productID"]').attr('content');
                  };
                  return _this.initProductEl($('#imageZoomer'), {
                    productSid: currentSid()
                  }, {
                    initOverlay: function(overlay) {
                      overlay.addAlwaysShow('productPage');
                      overlay.autoFixPosition();
                      return overlay.el.css('zIndex', 100000);
                    }
                  });
                }
              });
            };
          })(this));
        };

        return MacysSiteInjector;

      })(SiteInjector);
    }
  };
});

//# sourceMappingURL=MacysSiteInjector.js.map
