// Generated by CoffeeScript 1.10.0
var extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty;

define(function() {
  return {
    d: ['DataDrivenSiteInjector'],
    c: function() {
      var BloomingdalesSiteInjector;
      return BloomingdalesSiteInjector = (function(superClass) {
        extend(BloomingdalesSiteInjector, superClass);

        function BloomingdalesSiteInjector() {
          return BloomingdalesSiteInjector.__super__.constructor.apply(this, arguments);
        }

        BloomingdalesSiteInjector.prototype.productListing = {
          mode: 2,
          image: 'a img[src^="http://images.bloomingdales.com/is/image/BLM/"]',
          position: function(el) {
            var parent;
            parent = el.parents('.productImages');
            if (parent.length) {
              return parent;
            } else {
              return el;
            }
          },
          overlayZIndex: 999,
          productSid: function(href, a, img) {
            var matches;
            matches = href.match(/[?&]ID=(.*?)(?:$|&|#)/);
            if (matches) {
              return matches[1];
            }
          }
        };

        BloomingdalesSiteInjector.prototype.productPage = {
          mode: 2,
          initPage: function() {
            var i, len, results, sku, skus;
            skus = JSON.parse($('body').html().match(/BLOOMIES.pdp.upcmap\["\d*"\] = (\[[^\]]*\])/)[1]);
            this.map = {};
            this.colorMap = {};
            results = [];
            for (i = 0, len = skus.length; i < len; i++) {
              sku = skus[i];
              if (!this.colorMap[sku.color]) {
                this.colorMap[sku.color] = sku.upcID;
              }
              results.push(this.map[sku.color + "." + sku.size] = sku.upcID);
            }
            return results;
          },
          productSid: function() {
            var color, id, size;
            id = document.location.href.match(/[?&]ID=(.*?)(?:$|&|#)/)[1];
            color = $('#colorHeadersDiv .pdpColorDesc').html().trim();
            size = $('.pdp_member_size .pdpSizeDesc').html().trim();
            return id + '-' + this.colorMap[color];
          },
          image: '#productImage',
          attach: '#pdpContainer'
        };

        return BloomingdalesSiteInjector;

      })(DataDrivenSiteInjector);
    }
  };
});

//# sourceMappingURL=BloomingdalesSiteInjector.js.map
