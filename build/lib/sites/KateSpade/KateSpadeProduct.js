// Generated by CoffeeScript 1.10.0
var extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty;

define(['scraping/SiteProduct', 'util'], function(SiteProduct, util) {
  var KateSpadeProduct;
  return KateSpadeProduct = (function(superClass) {
    extend(KateSpadeProduct, superClass);

    function KateSpadeProduct() {
      return KateSpadeProduct.__super__.constructor.apply(this, arguments);
    }

    KateSpadeProduct.prototype.images = function(cb) {
      return this.product["with"]('more', (function(_this) {
        return function(more) {
          var color, colorId, colorName, i, images, img, imgs, len, name, parts, ref, ref1;
          images = {};
          parts = _this.product.get('productSid').split('_');
          colorId = parts[1];
          colorName = null;
          ref = more.colors;
          for (i = 0, len = ref.length; i < len; i++) {
            color = ref[i];
            if (color.id === colorId) {
              colorName = color.name;
              break;
            }
          }
          ref1 = more.images;
          for (name in ref1) {
            imgs = ref1[name];
            images[name] = (function() {
              var j, len1, results;
              results = [];
              for (j = 0, len1 = imgs.length; j < len1; j++) {
                img = imgs[j];
                results.push({
                  small: img + '?op_sharpen=1&resMode=sharp2&id=kmVqf0&scl=10&fmt=jpg',
                  medium: img + '?op_sharpen=1&resMode=sharp2&id=kmVqf0&scl=6&fmt=jpg',
                  large: img + '?op_sharpen=1&resMode=sharp2&id=kmVqf0&scl=3&fmt=jpg',
                  larger: img + '?op_sharpen=1&resMode=sharp2&id=kmVqf0&scl=2&fmt=jpg',
                  full: img + '?op_sharpen=1&resMode=sharp2&id=kmVqf0&scl=1&fmt=jpg'
                });
              }
              return results;
            })();
          }
          return cb(images, colorName);
        };
      })(this));
    };

    KateSpadeProduct.prototype.widgets = function(cb) {
      return this.product["with"]('more', 'reviews', (function(_this) {
        return function(more, reviews) {
          var widgets;
          widgets = _this.genWidgets(more, {
            description: 'Description',
            sizes: 'Sizes',
            'details.details': 'Details',
            'details.features': 'Features',
            'details.material': 'Material',
            colors: {
              title: 'Colors',
              map: function(color) {
                return util.ucfirst(color.name);
              }
            },
            reviews: {
              obj: reviews
            }
          });
          return cb(widgets);
        };
      })(this));
    };

    return KateSpadeProduct;

  })(SiteProduct);
});

//# sourceMappingURL=KateSpadeProduct.js.map
