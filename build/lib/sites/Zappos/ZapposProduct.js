// Generated by CoffeeScript 1.7.1
var __hasProp = {}.hasOwnProperty,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

define(['scraping/SiteProduct'], function(SiteProduct) {
  var ZapposProduct;
  return ZapposProduct = (function(_super) {
    __extends(ZapposProduct, _super);

    function ZapposProduct() {
      return ZapposProduct.__super__.constructor.apply(this, arguments);
    }

    ZapposProduct.prototype.baseUrl = 'http://www.zappos.com';

    ZapposProduct.prototype.images = function(cb) {
      return this.product["with"]('more', (function(_this) {
        return function(more) {
          var id, image, images, name, style, _ref, _ref1;
          images = {};
          _ref = more.styles;
          for (id in _ref) {
            style = _ref[id];
            images[id] = [];
            _ref1 = style.images;
            for (name in _ref1) {
              image = _ref1[name];
              if (image.MULTIVIEW) {
                images[id].push({
                  small: image.MULTIVIEW,
                  medium: image.MULTIVIEW,
                  large: image['4x'],
                  larger: image['4x'],
                  full: image['4x']
                });
              }
            }
          }
          return cb(images, more.currentStyle);
        };
      })(this));
    };

    ZapposProduct.prototype.widgets = function(cb) {
      return this.product["with"]('more', 'reviews', (function(_this) {
        return function(more, reviews) {
          var colors, id, style, widgets, _ref;
          colors = [];
          _ref = more.styles;
          for (id in _ref) {
            style = _ref[id];
            colors.push(style.color.name);
          }
          widgets = _this.genWidgets(more, {
            description: 'Description',
            sizes: 'Sizes',
            colors: {
              obj: colors,
              title: 'Colors'
            },
            reviews: {
              obj: reviews
            }
          });
          return cb(widgets);
        };
      })(this));
    };

    return ZapposProduct;

  })(SiteProduct);
});

//# sourceMappingURL=ZapposProduct.map
