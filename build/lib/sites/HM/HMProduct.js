// Generated by CoffeeScript 1.10.0
var extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty;

define(['scraping/SiteProduct'], function(SiteProduct) {
  var HMProduct;
  return HMProduct = (function(superClass) {
    extend(HMProduct, superClass);

    function HMProduct() {
      return HMProduct.__super__.constructor.apply(this, arguments);
    }

    HMProduct.prototype.images = function(cb) {
      return this.product["with"]('more', (function(_this) {
        return function(more) {
          var color, colorImages, image, images, ref;
          images = {};
          ref = more.images;
          for (color in ref) {
            colorImages = ref[color];
            images[color] = (function() {
              var i, len, results;
              results = [];
              for (i = 0, len = colorImages.length; i < len; i++) {
                image = colorImages[i];
                results.push({
                  small: image,
                  medium: image,
                  large: image,
                  larger: image,
                  full: image
                });
              }
              return results;
            })();
          }
          return cb(images, more.color);
        };
      })(this));
    };

    HMProduct.prototype.widgets = function(cb) {
      return this.product["with"]('more', (function(_this) {
        return function(more) {
          var widgets;
          widgets = _this.genWidgets(more, {
            description: 'Description',
            details: 'Details',
            sizes: 'Sizes',
            colors: {
              title: 'Colors',
              map: function(color) {
                return color.name;
              }
            }
          });
          return cb(widgets);
        };
      })(this));
    };

    return HMProduct;

  })(SiteProduct);
});

//# sourceMappingURL=HMProduct.js.map
