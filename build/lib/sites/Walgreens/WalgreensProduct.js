// Generated by CoffeeScript 1.10.0
var extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty;

define(['scraping/SiteProduct', 'underscore'], function(SiteProduct, _) {
  var WalgreensProduct;
  return WalgreensProduct = (function(superClass) {
    extend(WalgreensProduct, superClass);

    function WalgreensProduct() {
      return WalgreensProduct.__super__.constructor.apply(this, arguments);
    }

    WalgreensProduct.prototype.images = function(cb) {
      return this.product["with"]('more', (function(_this) {
        return function(more) {
          var i, id, image, len, otherImages, ref, urlFirst, us;
          otherImages = [];
          ref = more.images;
          for (i = 0, len = ref.length; i < len; i++) {
            image = ref[i];
            id = image.match(/http:\/\/pics\.drugstore\.com\/prodimg\/([^\/]*)/)[1];
            urlFirst = "http://pics.drugstore.com/prodimg/" + id + "/";
            image = image.match(/http:\/\/pics\.drugstore\.com\/prodimg\/[^\/]*\/([\S\s]*?)\.jpg/)[1];
            us = image.match(/(_)/);
            if (us) {
              otherImages.push({
                small: urlFirst + image.match(/([^_]*)/)[1] + "_50.jpg",
                medium: urlFirst + image.match(/([^_]*)/)[1] + "_220.jpg",
                large: urlFirst + image.match(/([^_]*)/)[1] + "_450.jpg",
                larger: urlFirst + image.match(/([^_]*)/)[1] + "_450.jpg",
                full: urlFirst + image.match(/([^_]*)/)[1] + "_450.jpg"
              });
              console.log('qwet');
              console.log(otherImages);
            } else {
              otherImages.push({
                small: urlFirst + "50.jpg",
                medium: urlFirst + "220.jpg",
                large: urlFirst + "450.jpg",
                larger: urlFirst + "450.jpg",
                full: urlFirst + "450.jpg"
              });
            }
          }
          return cb({
            '': otherImages
          }, '');
        };
      })(this));
    };

    WalgreensProduct.prototype.widgets = function(cb) {
      return this.product["with"]('more', (function(_this) {
        return function(more) {
          var widgets;
          widgets = _this.genWidgets(more, {
            overview: 'Overview',
            description: 'Description',
            ingredients: 'Ingredients',
            uses: 'Uses',
            warnings: 'Warnings'
          });
          return cb(widgets);
        };
      })(this));
    };

    return WalgreensProduct;

  })(SiteProduct);
});

//# sourceMappingURL=WalgreensProduct.js.map
