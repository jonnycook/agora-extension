// Generated by CoffeeScript 1.10.0
var extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty;

define(['scraping/SiteProduct', 'underscore', 'util'], function(SiteProduct, _, util) {
  var NordstromProduct;
  return NordstromProduct = (function(superClass) {
    extend(NordstromProduct, superClass);

    function NordstromProduct() {
      return NordstromProduct.__super__.constructor.apply(this, arguments);
    }

    NordstromProduct.prototype.images = function(cb) {
      return this.product["with"]('more', (function(_this) {
        return function(more) {
          var images, otherImages;
          otherImages = _.map(more.images, function(image) {
            return {
              small: image.replace(/mini/i, 'thumbnail'),
              medium: image.replace(/mini/i, 'large'),
              large: image.replace(/mini/i, 'large'),
              larger: image.replace(/mini/i, 'large'),
              full: image.replace(/mini/i, 'zoom')
            };
          });
          images = _.mapValues(more.colorImages, function(image) {
            return [
              {
                small: image,
                medium: image.replace(/thumbnail/i, 'large'),
                large: image.replace(/thumbnail/i, 'large'),
                larger: image.replace(/thumbnail/i, 'large'),
                full: image.replace(/thumbnail/i, 'zoom')
              }
            ].concat(otherImages);
          });
          return cb(images, more.color);
        };
      })(this));
    };

    NordstromProduct.prototype.widgets = function(cb) {
      return this.product["with"]('more', 'reviews', (function(_this) {
        return function(more, reviews) {
          var widgets;
          widgets = _this.genWidgets(more, {
            description: 'Description',
            details: 'Details',
            sizeInfo: 'Size Info',
            sizes: 'Sizes',
            colors: {
              title: 'Colors',
              map: function(color) {
                return util.ucfirst(color.name);
              }
            },
            reviews: {
              obj: reviews,
              map: {
                review: 'content'
              }
            }
          });
          return cb(widgets);
        };
      })(this));
    };

    return NordstromProduct;

  })(SiteProduct);
});

//# sourceMappingURL=NordstromProduct.js.map
