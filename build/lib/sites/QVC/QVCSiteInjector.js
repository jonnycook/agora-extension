// Generated by CoffeeScript 1.10.0
var extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty;

define(function() {
  return {
    d: ['DataDrivenSiteInjector'],
    c: function() {
      var QVCSiteInjector;
      return QVCSiteInjector = (function(superClass) {
        extend(QVCSiteInjector, superClass);

        function QVCSiteInjector() {
          return QVCSiteInjector.__super__.constructor.apply(this, arguments);
        }

        QVCSiteInjector.prototype.productListing = {
          imgSelector: 'a img[src^="http://images.qvc.com/is/image/"]',
          productSid: function(href, a, img) {
            var name, ref;
            name = (ref = href.match(/^http:\/\/www\.qvc\.com\/.*?\.product\.([^\.]+)/)) != null ? ref[1] : void 0;
            return name;
          }
        };

        QVCSiteInjector.prototype.productPage = {
          initPage: function() {
            var globalMatches, i, len, match, matches, results;
            this.colorMap = {};
            matches = document.head.innerHTML.match(/var arrSizeValues = new Array([\S\s]*?)<\/script>/);
            globalMatches = matches[1].match(/arrSizeValues\[.*?\]\[.*?\]="(.*?)"/g);
            results = [];
            for (i = 0, len = globalMatches.length; i < len; i++) {
              match = globalMatches[i];
              match = match.match(/arrSizeValues\[.*?\]\[.*?\]="(.*?)"/)[1];
              matches = match.match(/^([^:]*):[^:]*:[^:]*:([^:]*):[^:]*:([^:]*)$/);
              results.push(this.colorMap[matches[3]] = matches[1]);
            }
            return results;
          },
          test: function() {
            return $('#imageID').length;
          },
          productSid: function() {
            var id;
            id = document.location.href.match(/^http:\/\/www\.qvc\.com\/.*?\.product\.([^\.]+)/)[1];
            id += '-' + this.colorMap[$('#spanSelectedSizeColor').text().trim()];
            return id;
          },
          imgEl: '#imageID',
          waitFor: '#imageID'
        };

        return QVCSiteInjector;

      })(DataDrivenSiteInjector);
    }
  };
});

//# sourceMappingURL=QVCSiteInjector.js.map
