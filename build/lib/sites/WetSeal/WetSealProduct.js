// Generated by CoffeeScript 1.10.0
var extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty;

define(['scraping/SiteProduct', 'util'], function(SiteProduct, util) {
  var WetSealProduct;
  return WetSealProduct = (function(superClass) {
    extend(WetSealProduct, superClass);

    function WetSealProduct() {
      return WetSealProduct.__super__.constructor.apply(this, arguments);
    }

    WetSealProduct.prototype.images = function(cb) {
      return this.product["with"]('more', (function(_this) {
        return function(more) {
          var allImages, colorId, image, images, ref;
          allImages = {};
          ref = more.images;
          for (colorId in ref) {
            images = ref[colorId];
            allImages[colorId] = (function() {
              var i, len, results;
              results = [];
              for (i = 0, len = images.length; i < len; i++) {
                image = images[i];
                results.push({
                  small: image,
                  medium: image,
                  large: image,
                  larger: image,
                  full: image
                });
              }
              return results;
            })();
          }
          return cb(allImages, _this.product.get('productSid').split('_')[1]);
        };
      })(this));
    };

    WetSealProduct.prototype.widgets = function(cb) {
      return this.product["with"]('more', 'reviews', (function(_this) {
        return function(more, reviews) {
          var widgets;
          widgets = _this.genWidgets(more, {
            description: 'Description',
            sizes: 'Sizes',
            details: 'Details',
            colors: {
              title: 'Colors',
              map: function(color) {
                return util.ucfirst(color.name);
              }
            },
            reviews: {
              obj: reviews,
              map: {
                review: 'content'
              }
            }
          });
          return cb(widgets);
        };
      })(this));
    };

    return WetSealProduct;

  })(SiteProduct);
});

//# sourceMappingURL=WetSealProduct.js.map
