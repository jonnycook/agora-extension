// Generated by CoffeeScript 1.10.0
var extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty;

define(['scraping/SiteProduct', 'util'], function(SiteProduct, util) {
  var WomanWithinProduct;
  return WomanWithinProduct = (function(superClass) {
    extend(WomanWithinProduct, superClass);

    function WomanWithinProduct() {
      return WomanWithinProduct.__super__.constructor.apply(this, arguments);
    }

    WomanWithinProduct.prototype.images = function(cb) {
      return this.product["with"]('more', (function(_this) {
        return function(more) {
          var color, i, image, images, len, otherImages, ref;
          images = {};
          otherImages = (function() {
            var i, len, ref, results;
            ref = more.images;
            results = [];
            for (i = 0, len = ref.length; i < len; i++) {
              image = ref[i];
              results.push({
                small: image,
                medium: image,
                large: image,
                larger: image,
                full: image
              });
            }
            return results;
          })();
          ref = more.colors;
          for (i = 0, len = ref.length; i < len; i++) {
            color = ref[i];
            images[color.name] = [
              {
                small: color.image,
                medium: color.image,
                large: color.image,
                larger: color.image,
                full: color.image
              }
            ].concat(otherImages);
          }
          return cb(images, more.color);
        };
      })(this));
    };

    WomanWithinProduct.prototype.widgets = function(cb) {
      return this.product["with"]('more', 'reviews', (function(_this) {
        return function(more, reviews) {
          var widgets;
          widgets = _this.genWidgets(more, {
            description: 'Description',
            sizes: 'Sizes',
            colors: {
              title: 'Colors',
              map: function(color) {
                return util.ucfirst(color.name);
              }
            },
            reviews: {
              obj: reviews
            }
          });
          return cb(widgets);
        };
      })(this));
    };

    return WomanWithinProduct;

  })(SiteProduct);
});

//# sourceMappingURL=WomanWithinProduct.js.map
