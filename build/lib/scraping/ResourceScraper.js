// Generated by CoffeeScript 1.7.1
define(function() {
  var ResourceScraper;
  return ResourceScraper = (function() {
    function ResourceScraper(args) {
      if (this === window) {
        return {
          "const": args.callee,
          args: args,
          config: function(config) {
            return {
              "const": args.callee,
              args: args,
              config: config
            };
          }
        };
      }
    }

    ResourceScraper.prototype.config = function(config) {
      var name, value;
      for (name in config) {
        value = config[name];
        this[name] = value;
      }
      return this;
    };

    ResourceScraper.prototype.pushResource = function(resource) {
      return this.resource = resource;
    };

    return ResourceScraper;

  })();
});

//# sourceMappingURL=ResourceScraper.map
