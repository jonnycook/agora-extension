// Generated by CoffeeScript 1.10.0
define(function() {
  return function() {
    var Debug;
    return Debug = (function() {
      function Debug() {}

      Debug.stackTrace = function() {
        var trace;
        trace = Error().stack;
        trace = trace.split("\n").slice(3, -2);
        return _.map(trace, function(line) {
          var matches;
          matches = line.match(/at (.*?) \(/);
          if (matches) {
            return matches[1];
          }
        });
      };

      Debug.log = function() {
        var args, caller, i, ignoreList, item, j, k, len, len1, list, matched, trace;
        ignoreList = [['triggerBackgroundEvent', 'DeleteView'], ['triggerBackgroundEvent'], ['callBackgroundMethod'], [/^listen/], [/^stopListening/], ['CLIENT:'], [/^Process/], ['ConnectView'], ['ReceivedRequest']];
        for (j = 0, len = ignoreList.length; j < len; j++) {
          list = ignoreList[j];
          matched = true;
          for (i = k = 0, len1 = list.length; k < len1; i = ++k) {
            item = list[i];
            if (typeof item === 'string') {
              if (arguments[i] !== item) {
                matched = false;
              }
            } else {
              if (!item.exec(arguments[i])) {
                matched = false;
              }
            }
            if (!matched) {
              break;
            }
          }
          if (matched) {
            return;
          }
        }
        trace = this.stackTrace();
        args = Array.prototype.slice.call(arguments, 0, arguments.length);
        caller = trace[1];
        args.unshift('CLIENT:');
        return console.debug.apply(console, args);
      };

      return Debug;

    })();
  };
});

//# sourceMappingURL=Debug.js.map
