// Generated by CoffeeScript 1.10.0
define(function() {
  return function() {
    var Observable;
    return Observable = (function() {
      function Observable() {}

      Observable.prototype.observe = function(event, observer) {
        if (this.observers == null) {
          this.observers = {};
        }
        if (this.observers[event]) {
          return this.observers[event].push(observer);
        } else {
          return this.observers[event] = [observer];
        }
      };

      Observable.prototype.trigger = function(event) {
        var i, len, observer, ref, results;
        if (this.observers[event]) {
          ref = this.observers[event];
          results = [];
          for (i = 0, len = ref.length; i < len; i++) {
            observer = ref[i];
            results.push(observer.apply(null, Array.prototype.slice.call(arguments, 1)));
          }
          return results;
        }
      };

      return Observable;

    })();
  };
});

//# sourceMappingURL=Observable.js.map
