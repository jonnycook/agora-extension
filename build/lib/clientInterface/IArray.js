// Generated by CoffeeScript 1.7.1
define(['underscore'], function(_) {
  var IArray;
  return IArray = (function() {
    function IArray() {
      this._array = [];
      this.length = 0;
    }

    IArray.prototype.get = function(pos) {
      return this._array[pos];
    };

    IArray.prototype.setArray = function(array) {
      var el, i, _i, _len, _results;
      while (this.length) {
        this["delete"]();
      }
      _results = [];
      for (i = _i = 0, _len = array.length; _i < _len; i = ++_i) {
        el = array[i];
        _results.push(this.insert(el, i));
      }
      return _results;
    };

    IArray.prototype["delete"] = function(pos) {
      this._array.splice(pos, 1);
      return --this.length;
    };

    IArray.prototype.insert = function(el, pos) {
      if (pos === 0) {
        this._array.unshift(el);
      } else if (pos === this._array.length) {
        this._array.push(el);
      } else {
        this._array.splice(pos, 0, el);
      }
      return ++this.length;
    };

    IArray.prototype.push = function(el) {
      return this._array.insert(el, this._array.length);
    };

    IArray.prototype.each = function(cb) {
      return _.each(this._array, cb);
    };

    IArray.prototype.forEach = function(cb) {
      return this.each(cb);
    };

    IArray.prototype.move = function(from, to) {
      var el;
      if (from !== to) {
        el = this._array.splice(from, 1)[0];
        return this._array.splice(to, 0, el);
      }
    };

    return IArray;

  })();
});

//# sourceMappingURL=IArray.map
