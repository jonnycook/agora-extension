// Generated by CoffeeScript 1.10.0
var slice = [].slice;

define(['underscore'], function(_) {
  var Event;
  return Event = (function() {
    function Event() {}

    Event.prototype.subscribe = function(subscriber) {
      if (this.subscribers == null) {
        this.subscribers = [];
      }
      return this.subscribers.push(subscriber);
    };

    Event.prototype.unsubscribe = function(subscriber) {
      if (this.subscribers) {
        return _.pull(this.subscribers, subscriber);
      }
    };

    Event.prototype.fire = function() {
      var args, i, len, ref, results, subscriber;
      args = 1 <= arguments.length ? slice.call(arguments, 0) : [];
      if (this.subscribers) {
        ref = this.subscribers;
        results = [];
        for (i = 0, len = ref.length; i < len; i++) {
          subscriber = ref[i];
          results.push(subscriber.apply(null, args));
        }
        return results;
      }
    };

    return Event;

  })();
});

//# sourceMappingURL=Event.js.map
